import React, { ReactElement } from 'react';
import './App.css'

interface Props {
    
}

export default function Cell({}: Props): ReactElement {
    return (
        <div className = "cell">
            
        </div>
    )
}
